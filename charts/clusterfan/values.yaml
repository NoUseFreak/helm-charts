# Default values for clusterfan.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ghcr.io/nousefreak/clusterfan
  tag:

pwm:
  hostname: "kmaster"
